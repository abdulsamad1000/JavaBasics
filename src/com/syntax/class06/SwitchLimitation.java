package com.syntax.class06;

public class SwitchLimitation {

	public static void main(String[] args) {
		
		/*
		 * switch has a data type limitaions
		 * switch has a datatype limitations
		 * 
		 * switch can not work with double, float, long and boolean
		 */
		
		double price=10;
		
		
		/* switch (price) {-> can not switch on a value of type double.
		 }
		 */
		
		// switch has operators limitaions 
		// we can not use a logical operators inside switch
		
		char choice='Y';
		String meaning;
		
		// case 'Y' || 'Y': ---> CAN NOT USE ||
		
		
		}
		
	}


